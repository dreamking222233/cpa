**阻断问题**
- `internal/api/handlers/management/auth_files.go:1254` 期望通过 `h.authManager.Update()` 捕获单条更新失败并回填到 `failed`，但 `sdk/cliproxy/auth/conductor.go:1161` 里的 `Manager.Update()` 实际上无论持久化是否失败都会返回 `nil`，因为它把 `m.persist(ctx, auth)` 的错误直接丢掉了（`sdk/cliproxy/auth/conductor.go:1189`），而 `persist` 本身是可能失败的（`sdk/cliproxy/auth/conductor.go:3881`）。结果是批量接口对这类真实失败永远报成功，既不能可靠实现“单个失败不影响其他账号”的结果上报，也不能可靠返回 `207`。当前测试也都用 `NewManager(nil, nil, nil)`，覆盖不到这个问题。

**其他检查结果**
- 路由权限本身没看到新增绕过；批量接口挂在 management 组内：`internal/api/server.go:707`，仍受 `internal/api/handlers/management/handler.go:148` 的鉴权中间件保护。
- 单个/批量状态语义目前一致；两者都复用 `applyAuthDisabledState()`：`internal/api/handlers/management/auth_files.go:1159`，禁用时写 `disabled/StatusDisabled/"disabled via management API"`，启用时恢复 `active` 并清空 `StatusMessage`。
- 管理页注入脚本没有看到明显 XSS 问题，渲染内容做了转义：`internal/api/management_auth_bulk_inject.go:68`。兼容/安全上有一个非阻断点：它会把 Management Key 持久化到 `localStorage`，而且写了两个 key（`internal/api/management_auth_bulk_inject.go:46`、`134`），这会扩大同源脚本可读范围，但和仓库里新加的其他管理页做法基本一致。
- 参数校验基础项是有的：`disabled` 必填、`names` 去空去重后不能为空，见 `internal/api/handlers/management/auth_files.go:1225`。
- 没发现这几处改动里的明显语法级编译错误，但当前环境没有 `go`/`gofmt`，无法实际编译验证。

## 修复记录

- 已在单个与批量启停流程中增加 `persistAuthRecordIfNeeded()`，在调用 `authManager.Update()` 前显式保存带 metadata 的认证记录。
- 批量接口遇到持久化失败会将该账号写入 `failed` 并继续处理其他账号，返回 `207 Multi-Status`。
- 已新增 `TestPatchAuthFilesStatusBatch_PersistFailureDoesNotDirtyRuntimeAuth`，覆盖持久化失败时运行时 auth 状态不被提前污染。
**Findings**
- 未发现阻断问题残留，也未发现本次修复在你限定范围内引入新的阻断问题。

**复审结论**
- 批量接口的“持久化失败要落到 `failed` 并返回 `207`”已修复。现在在更新 runtime 前先显式持久化，持久化失败会逐项写入 `failed`，最终只要存在失败项就返回 `207`：[`auth_files.go`](/Volumes/work_space/cpa/internal/api/handlers/management/auth_files.go:1275)、[`auth_files.go`](/Volumes/work_space/cpa/internal/api/handlers/management/auth_files.go:1296)。这正是对之前 `Manager.Update()` 吞掉持久化错误问题的补偿：[`conductor.go`](/Volumes/work_space/cpa/sdk/cliproxy/auth/conductor.go:1189)。
- “持久化失败不污染运行时 auth 状态”也成立。`findAuthByNameOrID()` 拿到的是 manager 的 clone，不是内部对象；`GetByID()` 和 `List()` 都返回 clone：[`conductor.go`](/Volumes/work_space/cpa/sdk/cliproxy/auth/conductor.go:2903)、[`conductor.go`](/Volumes/work_space/cpa/sdk/cliproxy/auth/conductor.go:2891)。`Auth.Clone()` 也复制了 `Attributes`/`Metadata` map：[`types.go`](/Volumes/work_space/cpa/sdk/cliproxy/auth/types.go:219)。对应回归测试已覆盖批量场景：[`auth_files_batch_test.go`](/Volumes/work_space/cpa/internal/api/handlers/management/auth_files_batch_test.go:307)。
- 单个启停路由仍在，处理链路也仍可用。路由注册没变：[`server.go`](/Volumes/work_space/cpa/internal/api/server.go:706)。handler 现在与批量一样先持久化、后 `Update()`，语义一致：[`auth_files.go`](/Volumes/work_space/cpa/internal/api/handlers/management/auth_files.go:1190)。
- 路由权限没有被放松。批量接口仍挂在 `/v0/management` 组下，并继续经过 availability middleware 和 management 鉴权 middleware：[`server.go`](/Volumes/work_space/cpa/internal/api/server.go:558)、[`handler.go`](/Volumes/work_space/cpa/internal/api/handlers/management/handler.go:148)。管理路由也仍只在存在 management secret/local password 时启用：[`server.go`](/Volumes/work_space/cpa/internal/api/server.go:318)。

**残余风险**
- 当前环境没有 `go`，我无法实际跑测试验证。
- 单个 `/auth-files/status` 没看到新增专门回归测试；这是测试覆盖空白，但按当前静态实现看，不构成新的阻断问题。
